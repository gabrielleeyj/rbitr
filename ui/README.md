# rbitr UI

The rbitr UI is a web-based interface for managing user controls.

## Development

```bash
npm install
npm run dev
```

The UI uses Vite + React + TypeScript + Radix UI. Set `VITE_API_BASE_URL` to point at the gateway
API (e.g., `http://localhost:8080`).
